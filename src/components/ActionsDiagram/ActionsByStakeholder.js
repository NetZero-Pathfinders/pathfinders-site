// import {
//   useSolutionsBySectorData,
//   SolutionsBySectorDiagram,
// } from "./ActionsBySector"

export default function SolutionsByStakeholder() {
  // const { sectors } = useSolutionsBySectorData({ type: "stakeholder" })
  if (!sectors.length) return null
  return null
  // return <SolutionsBySectorDiagram sectors={sectors} />
}
